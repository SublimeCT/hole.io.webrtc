# SPEC.md

需求现状的唯一真源。只记录"是什么"，不记录设计权衡讨论（那些放 `docs/decisions/`）。任何影响玩法规则、协议字段、界面流程的改动，必须在同一次提交里同步更新本文件，见 `AGENTS.md` 第 6 节。

状态标记：`✅ 已实现` `🚧 开发中` `📝 待实现`

## 玩法规则

| 项 | 值 | 状态 |
|---|---|---|
| 视角 | 俯视角，摄像机跟随玩家 | 📝 |
| 移动方式 | 触控/鼠标拖拽方向，无路径寻路 | 📝 |
| 洞的初始半径 | 待定 | 📝 |
| 吞噬判定 | 物体最小尺寸 ≤ 洞当前半径才能被吞噬，否则造成物体倾倒/位移但不消失 | 📝 |
| 倾倒物体处理 | 视为"活跃物体"，权威端临时跑物理模拟，静止后转回静态 | 📝 |
| 成长曲线 | 待定（吞噬后半径增长公式） | 📝 |
| 单局时长 | 待定 | 📝 |
| 单机 bot 数量/行为 | 待定 | 📝 |
| 联机房间人数上限 | 待定，需结合 host 上行带宽估算（参考 §联机 - 带宽） | 📝 |

## 单机模式

- 默认进入模式，无需网络。
- Bot AI：📝 待实现，行为模式待定（追逐/游荡状态机）。
- 使用 `packages/shared/simulation` 权威循环，本地直接跑，无广播开销。

## 联机模式

| 项 | 值 | 状态 |
|---|---|---|
| 架构 | Host 权威广播（星型拓扑），非全 mesh | 📝 |
| 建连流程 | WebSocket 信令交换 SDP/ICE → WebRTC DataChannel | 📝 |
| 打洞失败兜底 | TURN 中继（coturn，部署在阿里云轻量服务器） | 📝 |
| 输入包频率 | 约 30Hz，仅含方向向量，不含位置/分数 | 📝 |
| 快照广播频率 | 约 10Hz | 📝 |
| Host 掉线处理 | v1：房间解散，所有人回退单机继续（不做 host 迁移） | 📝 |
| 反作弊 | 不做，休闲游戏可接受 host 结构性作弊风险，见 `AGENTS.md` §8 | ✅ 决策已定 |

## 地图系统

| 项 | 值 | 状态 |
|---|---|---|
| 素材来源 | Kenney.nl / Quaternius 等 CC0 开源包，glTF 格式 | 📝 |
| 地图编辑器 | 网格吸附摆放预制件，导出/导入 JSON | 📝 |
| 地图数量（v1） | 待定 | 📝 |
| 程序化生成 | v1 不做，Phase 2 之后视情况引入 | 📝 |
| 每地图预制件规模 | 待定 | 📝 |

## 存档系统

| 项 | 值 | 状态 |
|---|---|---|
| 身份标识 | 客户端本地生成 `playerId` + `secret`，存 localStorage | 📝 |
| 存档内容 | 金币数量、已购皮肤、已购地图 | 📝 |
| 写入方式 | 事件化（如 `POST /save/coins { delta }`），不接受客户端直接上报最终数值 | ✅ 决策已定 |
| 商店内容 | 待定（皮肤/地图定价） | 📝 |

## 平台与设备

| 项 | 值 | 状态 |
|---|---|---|
| 目标设备 | 移动端（触控）+ 桌面端（鼠标） | 📝 |
| 最低性能目标 | 千元机移动浏览器可流畅运行 | ✅ 决策已定 |

## 变更记录

| 日期 | 变更 | 关联 Phase |
|---|---|---|
| 2026-07-19 | 初始 SPEC 建立，架构决策落定（技术栈、host 广播架构、存档事件化） | Phase 0 之前 |